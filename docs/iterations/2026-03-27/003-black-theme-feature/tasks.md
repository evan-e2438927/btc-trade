# Tasks

**Iteration**: 003-black-theme-feature
**Date**: 2026-03-27
**Mini**: Yes

---

## T-001: 修改 layout.tsx 全局背景为纯黑

**验收标准**:
- [x] `app/layout.tsx` body 的 `className` 中 `bg-gray-950` 改为 `bg-black`
- [x] 注释更新以反映纯黑背景

---

## T-002: 修改 page.tsx 主页面背景为纯黑

**验收标准**:
- [x] `app/page.tsx` 主 div 的 `className` 中 `bg-gray-950` 改为 `bg-black`

---

## T-003: 验证 UI 层次和可读性

**验收标准**:
- [x] 纯黑背景与面板灰阶形成清晰层次
- [x] 边框在纯黑背景上清晰可见
- [x] 白色文字在黑色背景上可读性良好
- [x] TradingView chart 独立渲染正常

---

**预估工时**: 5 分钟（纯 CSS 类名替换）
